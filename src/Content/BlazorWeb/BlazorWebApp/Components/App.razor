<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <link href="_content/Masa.Blazor/css/masa-blazor.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="css/app.css" />
    <link rel="stylesheet" href="BlazorWebApp.styles.css" />
    @*#if(useCDN)*@
    <link rel="stylesheet" href="https://cdn.masastack.com/npm/@("@mdi")/font@7.1.96/css/materialdesignicons.min.css" >
    @* <link rel="stylesheet" href="https://cdn.masastack.com/npm/materialicons/materialicons.css" > *@
    @* <link rel="stylesheet" href="https://cdn.masastack.com/npm/fontawesome/v6.4.0/css/all.min.css" > *@
    @*#else*@
    <link rel="stylesheet" href="css/materialdesign/v7.1.96/css/materialdesignicons.min.css" >
    @* <link rel="stylesheet" href="css/material/icons.css" > *@
    @* <link rel="stylesheet" href="css/fontawesome/v6.4.0/css/all.min.css" > *@
    @*#endif*@
    @*#if (SampleContent)
    <link rel="icon" type="image/png" href="favicon.png" />
    ##endif*@
    @*#if (!InteractiveAtRoot)
    <HeadOutlet />
    ##elseif (IndividualLocalAuth)
    <HeadOutlet @rendermode="@RenderModeForPage" />
    ##elseif (UseServer && UseWebAssembly)
    <HeadOutlet @rendermode="@InteractiveAuto" />
    ##elseif (UseServer)
    <HeadOutlet @rendermode="@InteractiveServer" />
    ##else
    <HeadOutlet @rendermode="@InteractiveWebAssembly" />
    ##endif*@
    @*#if (InteractiveAtRoot)
    ##elseif (UseServer && UseWebAssembly)
    <MSsrThemeProvider @rendermode="@InteractiveAuto" />
    ##elseif (UseServer)
    <MSsrThemeProvider @rendermode="@InteractiveServer" />
    ##elseif (UseWebAssembly)
    <MSsrThemeProvider @rendermode="@InteractiveWebAssembly" />
    ##else
    <MSsrThemeProvider />
    ##endif*@
</head>

<body>
    @*#if (!InteractiveAtRoot)
    <Routes />
    ##elseif (UseServer && UseWebAssembly)
    <Routes @rendermode="@InteractiveAuto" />
    ##elseif (UseServer)
    <Routes @rendermode="@InteractiveServer" />
    ##else
    <Routes @rendermode="@InteractiveWebAssembly" />
    ##endif*@
    @*#if (SampleContent && !InteractiveAtRoot)
    <script src="_content/Masa.Blazor/js/ssr-state-restore.js"></script>
    @*#if (UseServer && UseWebAssembly)
    <MSsrStateProvider @rendermode="@InteractiveAuto" />
    #elseif (UseServer)
    <MSsrStateProvider @rendermode="@InteractiveServer" />
    #elseif (UseWebAssembly)
    <MSsrStateProvider @rendermode="@InteractiveWebAssembly" />
    ##endif*@
    ##endif*@
    <script src="_framework/blazor.web.js"></script>
    <script src="_content/BlazorComponent/js/blazor-component.js"></script>
</body>

</html>